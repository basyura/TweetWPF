<ev:ViewBase x:Class="TweetWPF.Views.TweetWPFView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
        xmlns:ev="clr-namespace:Eleve;assembly=Eleve"
        xmlns:vm="clr-namespace:TweetWPF.ViewModels"
        xmlns:conv="clr-namespace:TweetWPF.Controls.Coverters"
        xmlns:ctrl="clr-namespace:TweetWPF.Controls"
        Title="TweetWPF" Height="600" Width="400">
    
    <ev:ViewBase.DataContext>
        <vm:TweetWPFViewModel />
    </ev:ViewBase.DataContext>

    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <ev:Execute Action="Initialize" />
        </i:EventTrigger>
    </i:Interaction.Triggers>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="{Binding Message}" TextAlignment="Left" Padding="5" />
        <TabControl Grid.Row="1">
            <TabItem Header="Home">
                <ctrl:TimelineView ItemsSource="{Binding Tweets}" />
            </TabItem>
            <TabItem Header="Mention">
                <ctrl:TimelineView ItemsSource="{Binding Mentions}" />
            </TabItem>
        </TabControl>
    </Grid>
</ev:ViewBase>
